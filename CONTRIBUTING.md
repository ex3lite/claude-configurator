# Contributing

1. Fork the repository and create a focused branch.
2. Run `go test -race ./...` and `go vet ./...`.
3. Keep changes small and preserve unknown Claude Code settings.
4. Open a pull request explaining the user-visible behavior.

Go 1.25 or newer is required.
